import { ImageAssetIcon, VideoAssetIcon, DocumentAssetIcon, CredentialsIcon } from 'app-icons'

const HERITAGES_ICONS = {
  IMAGE: ImageAssetIcon,
  VIDEO: VideoAssetIcon,
  DOCUMENT: DocumentAssetIcon,
  CREDENTIAL: CredentialsIcon,
}

export { HERITAGES_ICONS }
