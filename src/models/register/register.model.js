export class Register {
  constructor({ name, email, cpf, birthday, password, firstAccount }) {
    this.name = name
    this.email = email
    this.cpf = cpf
    this.birthday = birthday
    this.password = password
    this.firstAccount = firstAccount
  }
}
